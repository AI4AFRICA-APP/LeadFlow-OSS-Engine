# App Configuration
GEMINI_API_KEY=

# SMTP Configuration (Provider: Gmail, Outlook, Amazon SES, etc.)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM_EMAIL=
SMTP_FROM_NAME="LeadFlow OSS Engine"
# Optional blind copy on every send (omit for OSS / privacy)
SMTP_BCC=
